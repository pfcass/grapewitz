<!DOCTYPE html>
<html>

<head>
  <title>Grapewitz</title>
  <!--[if lt IE 9]>
  <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>

<body class='<%= controller.controller_name %>'>
<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="#">GrapeWitz</a>
      <div class="nav-collapse">
        <ul class="nav">
          <li><%= link_to "Browse Wines", wines_path %></li>
          <li><%= link_to "My Cellar", user_path(current_user) %></li>
        </ul>
      </div>
    </div>
  </div>
</div>
<div id="container">
  <div id="header">
    <% if notice != nil %>
        <p class="notice"><%= notice %></p>
    <% end %>

    <% if alert != nil %>
        <p class="alert"><%= alert %></p>
    <% end %>
    <% if current_user != nil %>
        <table>
          <tr>
            <td width="20%"><%= image_tag("grapes.jpg", size: "180x180") %></td>
            <td width="80%"><%= yield :header %></td>
          </tr>
        </table>
    <% end %>
  </div>

  <div class="row">
    <div class="span3">
      <div id="menu">
      <% if current_user != NIL %>
          <strong>You are logged in as</strong>

          <p><%= current_user.email %></p>

          <p class="logout"><%= link_to "Logout", destroy_user_session_path, method: :delete %>
            <%= link_to "Home", :root %>
            <%= link_to 'Back', :back %></p>
          <%= button_to "Wine List", wines_path, :class => 'button', :method => :get %>
          <hr>
          <p><strong>My Links</strong></p>

      <% end %>

      <%= yield :left_sidebar %>
      </div>
    </div>

    <div class="span9">
      <%= yield %>
    </div>

  </div>
  <div id="footer">
    <%= yield :footer %>
    Copyright © Casswitz Enterprises
  </div>

</div>

</body>
</html>
